# 中期记忆

> 最后更新：2026-09-08 | 层：项目周期（随项目演进） | 机制：现行

## 项目规则

### AI 平台 / ai-platform（monorepo）

- 仓库：github.com/201650545/ai-platform，物理根 `D:\Work\AI平台`，代码在 `apps/`
- 当前四个平级应用：`central-platform`(:8000)、`component-orchestrator`(:8791)、`search-gateway`(:3000)、`api-gateway`(:3100)
- docs 知识库在 `D:\Work\AI平台\docs\`，一服务一文件（`docs/design/AI基础设施/服务/*.md`）；ARCHITECTURE.md 已标"过时提示"，以 docs 为事实源
- 文档归属规则（Obsidian work）：服务文档真源放 Obsidian 单文件式，内容精简、长流水归档

### API 转发网关 api-gateway(:3100)

- 运行体：`D:\项目\ai-hub\search_gateway\services\api_gateway.py`（监听 :3100），前端 `web\api_page.html`
- 仓库副本：`D:\Work\AI平台\apps\api-gateway\services\api_gateway.py` + `web\api_page.html`——两处需同步改
- 配置三拆：`model_catalog.json`(是什么) / `model_routes.json`(怎么走) / `channel_registry.json`(怎么样)，另有 `quota.json`(calls/input_tokens/output_tokens/errors)
- 观测 API：`/api/health`、`/api/gateway-catalog`、`/api/usage`、`/api/rate-limits`、`/api/route-log`、`/api/route-plan`
- ADR-003 成员池：已有 members[]、30s 默认冷却；但 select_members 仍按原顺序返回，未实现"额度+延迟动态加权"（禁止假装已做）
- fallback：fallback_policy.enabled 默认关，备用链须显式声明；fallback_count 经 X-Fallback-Count 暴露

### 网关主题体系（四套世界观）

| 主题 | 中文名 | 数据-style | 性格 |
|---|---|---|---|
| 黑白建筑极简 | 玄白/MONO | mono(第6套) | 理性专业长期工作 |
| 云海天舟 | Cloudsea Ark | - | 东方山水流动空灵 |
| 星河枢机 | Celestial Relay | - | 深邃宏大智慧观测 |
| 月夜穹顶 | Midnight Dome | - | 西式暗色总控大厅 |

- 主题规范存 `D:\Work\AI平台\apps\api-gateway\docs\`（主题设置-*.md + 预览图）
- 落地方式：新增 data-style 走现有机制（data-theme 明暗+data-style 5套+gw-accent+localStorage），**不另起主题系统**
- 已落地：仅 mono（黑白建筑极简），其他三套待做

## 决策记录

- 记忆体系三件套（长期/中期/短期）是核心机制，按原始设计稿(dfd45f8)复用，不可归档
- Obsidian `调度大脑记忆/` 按 type 分类镜像 + 索引.md 总入口；访问三步：查新→补漏→挂链
- 主题 docs 跟进 app 的笔记走、不塞进代码目录；跨网关共享 ADR 留在 search-gateway 一份

## 工作流程

### 记忆机制工作流（原始设计，用当前口径）

1. 对话开始 → Agent 按序读取本地 长期→中期→短期
2. 对话结束前 → 改写本地 md（精炼摘要）；Obsidian 是总记忆，所有 agent 读同一处
3. 短期每次对话覆盖（保留延续待办）；中期项目结束归档清空；长期低频追加

### 主题/前端落地流程

1. 用户先出预览图定方向 → 再定向生成（流程 workflow_visual_preview_first）
2. 落地走 data-style，纯皮肤层，不伪造未实现逻辑
3. 交付只给文件路径

---
## 变更日志
- 2026-06-26 [新增] 初始化中期记忆（荷叶圆圆/二年级语文教案——已于 2026-09-08 归档进 git 历史，本地清空）
- 2026-09-08 [更新] 替换为 AI 平台 / API 转发网关项目规则 + 四主题体系 + 记忆工作流当前口径