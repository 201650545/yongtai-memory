---
name: project-ai-gateway
description: D:\项目\services\search_gateway 统一 AI 网关目录（:3000 搜索聚合 + :3100 API 转发，原 D:\游戏\ds_v4_cli 已迁移）——opencli 4 引擎 + LLM 渠道路由 + 统一模型组/光线拖拽编排/总开关/用量记账/派发中心二级页（GATEWAY_ID 陷阱）
metadata:
  node_type: memory
  type: project
  originSessionId: cc570cb5-bfc6-41c1-a63b-84533fa58583
  modified: 2026-09-01T00:41:14.311Z
---

统一 AI 搜索聚合网关位于 `D:\游戏\ds_v4_cli\`（2026-08-04 由郭老师交接文档驱动落地，同日升级为 v2 多渠道聚合站）。

**位置更新（2026-08-13）**：网关已迁移到 `D:\项目\services\search_gateway\`（runtime 实际运行位置），`D:\游戏\ds_v4_cli\` 是旧副本（两处 engines.py 内容一致）。引擎实际 5 个：`ENGINE_ORDER = ["yuanbao","doubao","kimi","qianwen","metaai"]`（4 个已登录可用，Meta AI 未登录）。

**架构（v2）**：`unified_gateway.py`(:3000，多 Tab 聚合站：渠道对话/AI 搜索/渠道管理)、
`channels.py`（LLM 渠道注册表+转发）、`engines.py`（opencli 浏览器会话适配层，4 引擎并发线程）、
`setup_engines.py`（会话绑定）、`channels.json`（网页填的免费渠道 key）、`README.md`。

**LLM 渠道实测（2026-08-04）**：DeepSeek 官方（env DEEPSEEK_API_KEY）、Google Gemini（env GOOGLE_API_KEY，
走 `generativelanguage.googleapis.com/v1beta/openai` OpenAI 兼容端点）、OpenRouter（key 从 openclaw 备份发现，
`sk-or-v1-…`，免费档 14+ 免费模型）三个已接通；Groq/硅基流动/通义/智谱为网页可填 key 槽位。
`/v1/chat/completions` 按模型名路由 + 失败自动 fallback（链 deepseek→openrouter→gemini→槽位）。

**引擎实测结论（关键，避免重蹈覆辙）**：
- 会话名可自动创建：`opencli browser <新会话名> open <url>` 即可建立；`tab new`/`open` 会切换会话默认标签页，health 变 about:blank 时重跑 setup_engines.py 恢复。
- **`keys Enter` 在这些 React AI 站点大多不提交**（元宝/豆包/秘塔都不行，Kimi 可以）。元宝必须点 `#yuanbao-send-btn`。
- **CDP `click` 对 React 发送按钮经常无效，必须用 JS `element.click()`**（秘塔 SendArrowButton、豆包 send-btn-wrapper 均如此）。
- **textarea 型输入框（豆包/秘塔）必须用 `type` 真实键入**，`fill` 不注册 React 受控状态，点发送会提交空消息；contenteditable 型（元宝/Kimi）用 `fill` 即可。
- 键入后需等 ~1s 让 React 启用发送按钮再点（SUBMIT_SETTLE_DELAY）。
- 元宝回答块选择器：`[class*=hyc-common-markdown-style]` 且不含 `-cot`（`.markdown-body` 在元宝匹配 0 个）。
- **豆包提交仍不稳**（React 偶发不注册，曾成功一次）——已知限制。
- Kimi 会弹会员促销弹窗，需先点「稍后再说」。
- **env 坑**：Bash/终端 shell 里 `DEEPSEEK_API_KEY` 可能是残留的 zscc 中转 key（`sk-RiLUT…DLBs`），非官方 key（`sk-564d…cd10`），启动网关前必须用官方 key 覆盖。

**opencli 命令引号坑**：`subprocess` 传 JS 时 cmd.exe 会截断换行（SyntaxError: Unexpected end of input），须用 `subprocess.list2cmdline` + 把 JS 换行替换为空格 + JS 内只用单引号。

**API 转发网关 :3100**（`api_gateway.py`，OpenAI 兼容 LLM 聚合转发，独立于 :3000 搜索）：12 渠道 fallback（opencode/modelscope/sensetime/agnes/zscc/deepseek/gemini/openrouter/groq/siliconflow/dashscope/zhipu），`route_completion` 按模型反查 `channels.model_providers` 构链逐个尝试、全挂返 502（规则 pin 全不可达时兜底 `DEFAULT_CHAIN`）。2026-08-20 新增三能力：①手动路由编排（"搭积木"）：`PUT /api/routing` 按模型指定 order+disabled，持久化 `data/search_gateway/routing.json`，`_apply_routing_rule` 在智能排序后重排（order 提前/智能补尾/disabled 剔除），前端 ▲▼+勾选排除，精确匹配模型名；②总开关（软）：`PUT /api/switch {enabled:false}` → `_handle_chat` 返 503 `gateway_paused`、服务仍在线，持久化 `api_state.json`；③转发用量面板：`GET /api/usage` 返回今日+累计+按渠道，前端顶部开关条+用量 sect（20s 刷新）。

**2026-08-21 前端 v3 大改版（api_page.html 全重写）**：①单屏 launcher 首页（html,body overflow:hidden，8 图标磁贴 hash 路由二级页：orchestrate/rules/test/channels/usage/log/access/api）；②全局字体放大（标题 24px/正文 15-16px）；③黑夜模式（<head> 内联脚本先设 data-theme 防闪烁，localStorage 'gw-theme' + prefers-color-scheme 回退，`:root[data-theme="dark"]` 令牌覆盖，🌙/☀️ 手动切换）；④渠道启停：channels.json `channel_enabled` map（缺 key=启用，启用=删 key 保持文件干净），`PUT /api/channels/<id>/enabled`，`get_channel_enabled/set_channel_enabled/invalidate_channel_cache`，停用后 all_models/model_providers/route_completion 全跳过，health 响应经 `_augment_health` 注入 enabled+key_pool；⑤接入信息页 `GET /api/gateway-info`（`_lan_ip()` UDP connect 8.8.8.8 不发包取局域网 IP，auth_required:false 内网信任免 key）+ Cherry Studio/Python/curl 三段接入代码复制按钮；⑥静态图片 `GET /img/<name>`（`_serve_img` 是**模块级函数**，调用写 `_serve_img(self,path)` 而非 self._serve_img——写错会 AttributeError 空响应）。

**2026-08-24 前端 v4 液态玻璃改版（api_page.html 全重写）**：①液态玻璃设计系统——`.glass` 配方（backdrop-filter blur(28px) saturate(170%) + 半透明底 + 1px 亮边 + inset 顶部高光）、`.blobs` 3 个 accent 色 blur(90px) 光斑 z-index:-1 慢速漂移动画（给玻璃提供折射源，暗色降透明度）；②调色盘——`--accent/--accent2` 双色变量 + color-mix 派生 `--acc-soft/--acc-softer/--acc-glow`，PALETTES 8 组（极光蓝/星云紫/樱花粉/落日橙/翡翠绿/深海青/熔岩红/石墨灰），🎨 弹窗换色即时生效，localStorage 'gw-accent' JSON{a,b} + head 内联脚本先行应用防闪变；③图片嵌入形状 `.ic-shape`——玻璃渐变圆角容器，img absolute inset:0 object-fit:cover + mask-image linear-gradient(to bottom, #000 0% → 48% .62 → 94% 透明) = 顶部不透明底部渐隐，emoji `.fb` 垫底 + onerror 显出；④accent 接管功能色（按钮渐变/开关 on/聚焦环/选中项/链接/代码 chip/拖放指示线），语义色保留（状态 pill 绿红、付费红免费绿、禁测紫）。

**2026-08-24 自定义可用模型（model_overrides.json）**：channels.py 新增 overrides 层——`custom:[{name,channel,model}]`（别名→指定渠道+上游模型名，同名覆盖）+ `hidden:[模型名]`（列表隐藏但按名调用仍可路由）。注入点：all_models()（追加 custom provider + pop hidden）、model_providers()（别名精确命中时注入，cid 去重）；**api_gateway.aggregate_models 已重写为走 channels.all_models()**（/v1/models 与前端单一真源，owned_by=渠道 id 列表）。端点：GET /api/model-overrides、POST /api/model-overrides/custom、DELETE ?name=、PUT /api/model-overrides/hidden。前端第 9 磁贴「模型管理」(#/models)：添加表单（渠道下拉只列有 key 的+datalist 上游模型）+隐藏开关列表（隐藏项仍显示可撤销）。坑：modelscope 渠道 2026-08-24 起所有 chat 模型返回空壳（choices:null、id 空、usage 0），与别名功能无关，直连同样空壳——渠道侧问题未解。

**2026-08-24 首页三七分改版（郭老师拍板：留白美+形状要有名字）**：首页弃 3×3 图片宫格，改 `.h-split` 左右分栏——左七（`.h-main`）开放无框信息：hero 渐变大标题+轻胶囊总开关+无框排版核心数字（细分隔线）+「今日转发」概览（次调用/tokens/最活跃渠道，`loadHome()` 轮询 /api/usage）+「最近转发」6 条去向（/api/route-log）；右三（`.h-side`，min(30%,344px)）玻璃导航按钮，每个=小图+**中文名+一句说明**（解决"形状没名字"痛点）。玻璃只留给浮起元素（导航钮/开关/顶部控件），信息区全开放。**坑：首页改 `.home` 为 overflow-y:auto 可滚动**——单屏 overflow:hidden 时左栏内容超高会视觉叠压页脚；另浏览器会缓存 HTML，改版验证要带 `?v=N` cache-buster 重开。

**2026-08-24 统一模型组 + 光线编排（郭老师核心诉求：跨厂商归一名+光线拖拽定转发顺序）**：
- **后端**：`data/search_gateway/unified_models.json` = `{归一名:{display?,members:{渠道id:上游模型名}}}`（如 modelscope 的 `deepseek-ai/DeepSeek-V4-Flash-0731` 与 sensetime 的 `deepseek-v4-flash` 归一为 `deepseek-v4-flash`）。channels.py：`normalize_model_name`（小写/空格下划线转横杠）、`load/save/set/delete_unified_model`、`unified_suggest(q)`（按关键词扫各渠道在线模型供勾选）。注入：all_models() 以独立条目插入（unified:True，替换同名）；**model_providers() 精确名命中统一模型时直接返回成员链**（不再走子串扫描），route_completion 的逐尝试改名 `p2["model"]=real_model` 无需改动。端点：GET /api/unified、GET /api/unified/suggest?q=、POST /api/unified、DELETE /api/unified?name=（连带清 routing 规则）。
- **空壳响应兜底（关键）**：modelscope 等 chat 模型会返 HTTP 200 但 `choices:null` 的空壳——route_completion 非流式先缓冲 body，`_looks_like_shell(raw)`（JSON 解析失败/无 choices/有 error）记错误并**继续链上下一渠道**；好 body 包 `_BufferedResponse`（getheader/read 垫片）透传。
- **前端（导航 9→7，模型编排一页合一）**：`#/orchestrate` = 统一命名 pill 条（✨标记+🗑删除+＋新建）+ 建组表单（关键词「搜一下」→ 各渠道模型勾选列）+ 右侧**转发光线**（HTML5 DnD 拖玻璃芯片排序/拖入禁用池，插入指示线，300ms 防抖自动 PUT /api/routing，toast 已保存）+ 已编排一览 + 隐藏/别名管理收进 `<details class="adv">`。规则语义：beam 芯片=order、池芯片=disabled，「恢复自动」=DELETE /api/routing?model=。预置编排：fast = sensetime→agnes→xiaohongshu→openrouter（modelscope 禁用）；deepseek-v4-flash = sensetime 优先（modelscope 禁用）。
- **PS 5.1 BOM 坑（曾致 curl 52 空响应）**：`Set-Content -Encoding UTF8` 写 JSON 带 BOM，Python `json.loads` 拒 `﻿` → do_POST 未捕获异常直接断连。服务端已硬化 `json.loads(body.decode("utf-8-sig"))`+try/except→400；客户端写文件用 `[IO.File]::WriteAllText($p,$s,(New-Object System.Text.UTF8Encoding($false)))`。
- **opencli 截图坑**：`Page.captureScreenshot` 60s 超时且误报"native dialog"时，真因多是**标签页非前台**——先 `opencli browser <会话> tab select <pageId>` 激活即好（eval 不需要前台，别被"页面还活着"迷惑）。

**2026-08-24 渠道模型选择（三级页：已选置顶+全量目录+搜索）**：`data/search_gateway/channel_models.json` = `{渠道id:{selected:[模型名]}}`。语义：**无条目/空列表=未策展（对外全量），有条目=对外只暴露已选**；按名调用与统一组成员不受限（同 hidden 语义），unified_suggest 建组扫描也看全量。channels.py：`load/save_channel_models`、`get_channel_selection`（未策展返 None）、`set_channel_selection`（空=取消策展删条目）；`_augment_health` 注入 `sel_count`（渠道卡片显示"⭐ N 已选"）；all_models() 按策展过滤各渠道贡献。端点：GET/PUT `/api/channels/<cid>/models`（PUT selected 数组，空=还原）。前端：hash 三级路由 `#/channel/<cid>`（render() 按 `/` 切段，ALLV=VIEWS+['channel']）；渠道行整行可点击（测活/开关 stopPropagation）+ › 箭头；详情页 = 搜索框 + ⭐已选置顶芯片(✕) + 📦未选芯片(＋)，350ms 防抖 PUT + toast + load() 刷新。**坑：PowerShell 里 `curl | python -c "json.load(sys.stdin)"` 会因管道 BOM 报 Unexpected UTF-8 BOM——先 `-o` 落盘再用 `encoding='utf-8-sig'` 读**。

**2026-08-24 四套艺术风格主题（郭老师拍板：莫奈/梵高/留白水墨/现代画廊，各配 4 调色盘）**：`html[data-style="monet|vangogh|ink|modern"]` + `[data-theme]` 组合，每风格一个 `:root[data-style=…]` 块覆盖令牌——`--fh` 标题字体（衬线/黑体/楷体/粗无衬线）、`--radius/--radius-s` 形状（26px 圆润→8px 方正）、光斑透明度/速度、明暗两套底色。**图标随主题色变色（核心技巧）**：`<i class="icm" style="--im:url(/img/icon-x.png)">` 替代 `<img>`，`.icm` 用 `mask:var(--im) center/contain no-repeat` + `background:linear-gradient(135deg,var(--accent),var(--accent2))` + drop-shadow——PNG 透明图标自动吃双色渐变，换调色盘即变色。JS `STYLES` 对象 = 4 风格 × 4 板 `{n,a,b}`；`applyStyle` 换风格时若当前 accent 在新风格板里则保留、否则落风格默认；`buildSwatches()` 随 applyStyle 重建色板（曾丢 pal-row 生成致 swatches:0，修复=抽独立函数在 applyStyle 内调）。持久化 `gw-style` + `gw-accent` JSON{a,b}，head 内联脚本先行设 data-style 防闪变。🎨 弹窗 = 艺术风格 2×2 按钮 + 调色盘圆点。验证截图 D:\项目\_tmp\sg_style_*.png（4 风格）。

**2026-08-24 文字色随调色盘联动（郭老师：换色要配对应字体颜色才看得清）**：①16 板每板加 `t`（明亮主文字）/`td`（暗色主文字）——与 accent 色相呼应的深/浅中性色（如荧光绿盘 t=#1c2b22 绿墨、朱砂 t=#2c2320 暖墨）；`applyTextColors()` 设 `--t` 并用 `mixc()`（hex 线性混合）从主文字与 `--bg1` 派生 `--t2/--t3/--t4` 三层灰阶，挂 applyAccent + toggleTheme。②`--acc-t` 派生令牌：accent 用作**文字**时自动保证对比——明亮 `color-mix(accent 72%,#141b2e)`（往深墨混）、暗色 `color-mix(accent 84%,#fff)`（往白提亮）；全页 24 处 `color:var(--accent)` 已全部改 `var(--acc-t)`（border/background 用途不动）。验证：荧光绿明亮下链接自动深化 #059849、暗色提亮，截图 D:\项目\_tmp\sg_text_neon_*.png。

**2026-08-24 第五套风格「苹果 · 液态玻璃」（data-style="liquid"）**：visionOS 质感——`--radius:30px` 超圆角、SF Pro 字体、`.swatch` 果冻凸起内影。调色盘 = 苹果系统色 4 板（海湾蓝 #0a84ff/日落 #ff375f/薄荷 #30d158/葡萄紫 #bf5af2），各带 t/td 文字色。风格总数 5×明暗=10 外观。截图 D:\项目\_tmp\sg_style_liquid_*.png。

**2026-08-25 液态玻璃「流动感」改造（GPT-5.6 Extended 问诊方案落地，郭老师反馈"不太液态没流动感"）**：核心理念=液态≠持续动画，是"材质对交互的流体式响应"（静止安静→指针靠近才 energize）。①玻璃配方重写：blur 36→24 + saturate(175%) brightness(1.035)、白底降为 linear-gradient(180deg, 白.22→白.10)（暗色 .09→.03）让背景参与材质、border .42、四向 inset 高光；②FX 三层光学栈：每个 `.glass` 由 JS `fxEnhance()` 注入 `.gfx` 容器（absolute inset:0 overflow:hidden z-index:5 pointer-events:none，**其他 4 风格 display:none 零开销**）——`.gfx-e` 8px 折射边环（backdrop-filter:url(#lg-edge-warp) SVG feTurbulence baseFrequency=".006 .012"+feDisplacementMap scale=34 **静态永不动画**，mask content-box XOR exclude 挖环，::before conic 菲涅尔高光由 `--edge-rot` 随指针转向，**禁止无限旋转**）、`.gfx-g` 220px 指针光斑（固定渐变只动 transform + mix-blend screen）、`.gfx-s` 进入扫光（WAAPI 每次进入只扫一次，禁无限循环）；③弹簧交互引擎：pointermove 只存目标值→rAF lerp（k=.14/.12）写 `--tx/--ty/--rx/--ry` 到 `perspective(900px) translate3d rotateX rotateY`，小元素倾斜阻尼 sc=min(1,w*h/40000)，离开后目标归零缓动回收、变量低于 .02 即 removeProperty 并停 rAF（**后台标签 rAF 节流属正常，前台才跑**）；④光斑液体场：`.blobs i` background:none 改由 ::before 承担（内层 border-radius 18s morph + 双 radial 渐变）+ ::after conic-gradient 26s linear 旋转 mix-blend screen（外层漂移+内层对流）；⑤性能红线：指针移动只动 transform/opacity（绝不 background/border-radius/filter/box-shadow），will-change 只在 .is-hot，prefers-reduced-motion 全关。SVG 滤镜在 body 末尾 `#lg-edge-warp`。验证：28 玻璃卡全注入、冒烟测试 hot/edge-rot/glint/sweep/弹簧写入全过、monet 下 gfx 隐藏、console 0 错误，截图 D:\项目\_tmp\sg_liquid2_*.png。**opencli 坑：复杂 JS eval 在 PowerShell 会被剥内层双引号（SyntaxError），必须用 Bash 工具跑**。

**2026-08-25 液态玻璃「配色架构」改造（GPT-5.6 Extended 问诊第二轮，郭老师反馈"全页蒙一层雾，应该换色系不是换滤镜"）**：病根=accent 三重角色（背景光斑+全部交互+文字色相）被 `saturate(175%)` 主动放大，且 `.blobs` 容器背景还烤着两条 accent 径向渐变（GPT 都没看到的第 4 放大器）。新架构=**accent 只是"色系种子"**：①玻璃恒中性——亮 `linear-gradient(白.46,白.29)+blur(22) saturate(118%) brightness(1.025)`，暗 `rgba(30,34,42,.54)→rgba(18,21,27,.40)+saturate(112%) brightness(.96)`（175→118 是单点最关键改动）；②光斑壁纸化——`--lg-wall-N:color-mix(in oklab,accent 26/20/10%,中性底)`，尺寸 34/28/23vmax、opacity .38/.30/.28（暗 .44/.34/.28）、blur(72px)、靠边角，容器 accent 渐变改 wall 派生色，上层叠中性白光层 `.blobs::after`（radial 白.72/.22，暗只剩白.055）=彩色环境光变"空气"；③文字恒中性——applyTextColors() 加 liquid 分支固定 `--t:#1b1d21`/暗 `#f2f3f5`（t2-t4 照旧 mixc 派生），**其他 4 风格仍走调色盘 t/td 不受影响**；④边环 saturate 135→118、blob ::before 删 saturate(135%)、::after conic 只剩白+微 accent2。色相只留给交互（按钮/开关/图标/链接/选中）。验收：落日→薄荷切换=只有空气和交互变色、底子文字玻璃全中性（"换空间配色"而非"换塑料片"），明暗截图 D:\项目\_tmp\sg_color_v16_*.png，monet 兼容 ✓，console 0。

**2026-08-25 渠道详情「全部模型」Cherry Studio 化（郭老师拍板：全量展示+按厂商分组折叠+全局搜索）**：①后端 channel_health 去掉全部截断——openrouter 不再只留免费`[:40]`（现 418 个全量）、modelscope/opencode 不再`[:20]`、deepseek/gemini 过滤删除，统一 `sorted(set(...))` 全量返回（**顺带解决了 #16 OpenRouter 模型恢复显示**）；②前端 `cdVendor()` 厂商推断：id 带 `org/` 前缀走 CD_ORGS 映射（含 `~org/` 前缀剥离——OpenRouter 有 12 个 `~anthropic/claude-*-latest` 别名 id），无前缀走 CD_KWS 关键词表（claude→Anthropic/qwen→通义/glm→智谱…50 条），未知归「其他模型」排最后；③分组渲染：品牌行（▼箭头+名+N 个模型）点击折叠/展开，`CD.fold` 按渠道+品牌记状态，**未选模型>60 个的渠道默认全收起**（foldDef），小渠道默认全展开；搜索同时匹配模型名+品牌名（搜"anthropic"出整组）且命中时强制展开。**坑：对象字面量键带空格未加引号（`openai oss:`）→整个 script SyntaxError 页面全瘫（CD undefined）；折叠 toggle 写成 `=foldDef` 应为 `=!current`（默认收起时首点必须展开）**。deepseek 401 为重启前既有问题（前后都是 7 在线），groq/siliconflow 本就是无 key 槽位。

**2026-08-25 液态玻璃「高级感」精修（郭老师反馈"主题还行但缺高级感"，三高杠杆改动）**：①**底色去默认化**——纯灰 `#f7f8fa/#eef1f5` 是"系统默认灰"显廉价，改冷石中性 `#f1f3f6/#e6e9ef`（暗色 `#0f1115/#0a0c10`）；②**玻璃边 hairline**——`.glass` border-color `.58`（粗白边读着廉价）→ `.16` 极细发丝线，投影拉长柔化（18px44→24px56、暗色 22→26px60），`--gborder` 同步降；③**字体锚点**——`.h-title` 30→38px+tracking-.035em、`.h-stat .v` 31→34、`.hl-num .v` 26→32（数字是页面"主角"要够大够稳，tabular-nums 已有）；④**渠道 chip 玻璃退让**——未选 chip 去玻璃成安静底 `color-mix(t 4%,transparent)+border line2+无 blur`，已选保留玻璃+accent 环（形成层级而非满屏磨砂）。原则：**高级感=底色微冷偏+边极细+字大而稳+玻璃只留给浮起/已选**，不是堆效果。已 push 到 ai-hub `refactor/monorepo-20260812` commit `7c66718`（仅 2 文件 surgical commit，channels.py 含 #17-#28 累积功能+目录去截断、api_page.html 含本次精修；channels.json/bases.json 未入库）。

**SenseNova U1 Fast 是图片模型不是对话模型（关键坑）**：`sensenova-u1-fast` 出现在 /v1/models 但走 chat/completions 必 404 "model is not found"。正确用法 `POST https://token.sensenova.cn/v1/images/generations` `{model,prompt,n:1,size}`，**size 必须用枚举**（1024x1024 会 400，最小 2048x2048；1664x2496/3072x1376 等 12 档），返回 `{data:[{url:OSS链接}]}`。**2026-08-24 图标体系重做（郭老师要求：无背景纯形状+风格统一）**：全部 10 张素材（icon-logo + 9 个 icon-导航名.png）用统一提示词模板重生成——蓝→紫渐变(#4F6BFF→#9B5CFF)扁平矢量风+纯白底隔离，PIL 抠白底成透明 PNG（四角采样+色距平滑 alpha+MinFilter 去白边），200px 落 `web/img/icon-*.png`，旧 logo.png/card-*.png 已删；前端 `.ni-img`/`.h-logo` 裸 img+drop-shadow 直接展示，不再套 .ic-shape 方块容器。**U1 三个坑**：①图里会画小水印（右下角小字），用「半透明+低饱和度→透明」规则清除（阈值 sat<42&a<210 安全；别对底部区域放宽到 sat<75&a<235——会把浅色图标内容误杀）；②提示词必须显式排除 frame/tile/card，否则会自作主张画白色圆角底框；③一次生成多个图标时个别会完全跑偏（如 log 生成了电路板方块），要逐张目检、跑偏的重生成。批量脚本 `D:\项目\_tmp\u1_regen_icons.py`（存在即跳过可续传，STYLE 模板+ICONS subjects 字典）。

**记账机制 + GATEWAY_ID 陷阱（跨网关通用，必读）**：`quota.py` 的 `record_call` 按日按渠道记 calls/input_tokens/output_tokens/errors 到 `data/search_gateway/<GATEWAY_ID>/quota.json`（90 天、文件锁）；`channels._QuotaResponse` 在转发响应被读取时自动解析 usage（流式/非流式）并记账——所以"没记账"往往是假象，真因是账本 id 混了或没暴露查询。**`channels.GATEWAY_ID = os.environ.get("GATEWAY_ID","ds_v4_cli")` 在 import channels 时即冻结**：要让某网关账本独立，必须在 `import channels` 之前 `os.environ.setdefault("GATEWAY_ID","<id>")`，否则账全混进默认 `ds_v4_cli`。:3100 现已 `setdefault("GATEWAY_ID","api_gateway")` 账本独立。

**三网关 + 中央平台路径**：`D:\项目\config\gateways.json` 注册 3 网关——search_gateway:3000 / api_gateway:3100 / canvas_orchestrator:8791（2026-08-19 已清理 ds_v4_cli/e2e_test_gw 重复门控）。中央平台 = `D:\项目\services\central\server.py`（FastAPI，读 `D:\项目\config\`，dashboard `:8000/dashboard/index.html`），**不是** `D:\项目\ai-hub\` 克隆仓库（后者 `PROJECT_DIR=BASE_DIR.parent` 读空 config，启动会错覆盖 :8000，已踩坑）。三服务统一由 `D:\项目\runtime\cli.py start --all` 管理（`config/runtime.yaml` 是唯一静态真源，只读）。

**runtime.yaml 拓扑（2026-08-21）**：4 服务——central:8000(order1)/search_gateway:3000(order2)/orchestrator:8791(order3)/**api_gateway:3100(order4)**（:3100 于 2026-08-21 补入 cli 管理，health=/api/health 而非 /health）。注意 cli 的 search_gateway 命令指向 `search_gateway.py`，但 :3000 实际跑的是 `unified_gateway.py`——手动 `Start-Process` 起的进程 cli status 显示"健康但无PID"（cli 只认它自己 spawn 的）。

**手动重启四网关的正确命令（2026-08-26 重启实录，别用错）**：①:3000 = `unified_gateway.py`（启动要等数秒才绑端口，连 8 引擎，别 4 秒内判失败）；②:3100 = `api_gateway.py`；③:8791 = **`canvas_server.py --serve-only`**（不是 orchestrator.py——那是编排库，直接跑会报「--topic 必填」退出；--serve-only 只起画布不要求主题）；④:8000 = `central/server.py`。全部 `Start-Process python <脚本绝对路径> -WorkingDirectory <所在目录> -WindowStyle Hidden` 起，判断成败一律以 `netstat -ano | findstr :<端口>` LISTENING 为准。

**职责精简（2026-08-20）**：:3000 回归纯 AI 搜索——`hub_page.html` 只留 7 搜索引擎卡片（yuanbao/doubao/kimi/qianwen/grok/perplexity/zai），删 4 个非搜索 widget（orchestrator_canvas/llm_chat/channel_status/memo_note）+ 编排/渠道 JS/CSS/预设；`unified_gateway.py` 删 mock 编排路由（/api/orchestrator/stream|start|confirm）+ 渠道管理路由（/api/channels GET + /api/channels/<id>/key|test），**保留全部 /v1/* + 搜索路由**（content_pool 内部调 /v1/chat/completions 做 DeepSeek 汇总、Chatbox 配 :3000/v1 依赖，故 /v1/* 不可删）。三网关职责分流：API 转发归 :3100、课件编排归 :8791。注意：旧后端进程 2672（Session 0/SYSTEM）已于 2026-08-21 用 UAC 提权 + taskkill 替换为新代码进程（当前 :3000 = unified_gateway.py 31480、:3100 = api_gateway.py 3696，均普通身份 `Start-Process` 起）。已实测 /v1/models 200、/health 200、POST /api/channels/<id>/key 与 /api/orchestrator/confirm 全 404、GET /api/search_aggregate 路由存活（真实搜索依赖引擎浏览器，可能慢）。**GATEWAY_ID 修复（2026-08-21）**：unified_gateway.py 已在 import channels 前 `os.environ.setdefault("GATEWAY_ID","search_gateway")`，注册表不再出现 ds_v4_cli 死条目（旧进程最后心跳后超时自清）。进程名与 PID 会随重启变化，判断状态一律以 `netstat -ano | findstr :3000` 为准。

**2026-08-25 渠道管理：新增/隐藏自定义渠道（#33）**：`channels.json` 新增 `custom_channels`（`{cid:{name,icon,base_url,env_key,models,billing_type,...}}`）+ `hidden_channels`（列表）。channels.py：`_config_cache` 惰性载入 + `_save_config` 写盘后必调 `_merge_custom_into_globals()`（把 custom_channels 塞回模块级 `CHANNELS`/`CHANNEL_ORDER`，**这样免改 ~20 处 `CHANNELS.get()` 调用点**，模块底部也调一次）；`ordered_channels()`=CHANNEL_ORDER 减 hidden，5 处展示/聚合循环 + health_all 全改用它；`channel_health()` 三个失败分支（无 key/HTTPError/Exception）必须 `return ch.get("models")` 而非 `[]`（否则停用/无 key 渠道模型全显示 0，即 #32）。端点：POST `/api/channels`（校验 id `[a-z0-9][a-z0-9._-]*` + base_url 必填，定义落 custom_channels，有 key 则同落 keys）、PUT `/api/channels/<cid>/hidden`、DELETE `/api/channels/<cid>`（只删 custom，内置返 400）。前端：`#/channels` 加「＋新增渠道」按钮 + `.ch-modal` 表单 + 「已隐藏」折叠区（`renderHidden()`，隐藏后可恢复），GET /api/health 增 `hidden` 字段。**深坑：重启网关必须杀掉"全部" api_gateway.py 进程**——`Start-Process` 起新进程时旧进程若没死干净，旧 listener 仍占 :3100 且跑旧代码，新端点（如 DELETE）返 404 而早一轮加的端点（POST/PUT）照常工作，极易误判"新代码没生效"。诊断：`Get-CimInstance Win32_Process -Filter "Name='python.exe'" | Where CommandLine -like '*api_gateway.py*'` 列出全部（曾同时两个 PID），逐个 Stop-Process 再起单个。

**2026-08-25 光线芯片点击换模型（#40）+ 全网关皮肤层（#36-39）+ 网关鉴权**：
- **光线芯片点击换模型**：api_page.html 点 .bchip（光线/禁用池均可）弹 #mp-modal——该渠道全量模型列表，📌「渠道已筛选·置顶」区在上、☰「全部目录」区在下，搜索过滤；点未筛选模型会先 PUT /api/channels/<cid>/models 加入 selected（与渠道管理双向同步）再 POST /api/unified 固定 members[cid]。纯前端零后端改动；MPICK 状态对象含 sel:Set。事件委托绑 #mp-list（模型名含斜杠引号，禁内联 onclick）。
- **全网关多风格皮肤层**：规范 `D:\项目\services\THEME_GUIDE.md`。方案=**注入不重写**：每页追加 token 覆盖 CSS + 右下角 🎨 悬浮切换器，默认一律保持现状、换肤后 localStorage 记住。落地：:3000 hub_page（apple/huawei/xiaomi/kawaii，内联 <style>+<script>）；:8791 canvas index+gallery（共享 skins.css/skins.js：midnight 星空/xiuxian 楷体宣纸+随机道语）；:8000 控制台 dashboard（skins.css+内联 JS：huawei/xiaomi/xiuxian/midnight）+ 导航首页 /（server.py f-string 内联 HTML 塞花括号要双写→改走 /static/nav-skins.{css,js} 静态文件）。kawaii 有点击爱心粒子、huawei 有网格纹理底。
- **网关 :3100 已启用 API key 鉴权**（用户在接入信息页开启并中途换过一次密钥）：`api_state.json` 的 api_key，守卫 `_require_auth` 盖 /api/* 与 /v1/*（页面/静态图豁免）；前端有全局 fetch 包装自动带 Authorization（401 弹 prompt 存 localStorage 'gw-api-key'）。**坑：换 key 后所有本地消费方全 401**——DSH settings.yaml 的 Authorization 必须手动同步新值（热重载即恢复）；排查 401 先怀疑 key 轮换而非代码。
- **提权重启 ：8000**：central server 进程 CommandLine 读不到且 Stop-Process 拒绝（Access denied）→ 自写 ps1（杀端口监听者+重启 server.py+轮询验证落日志文件）用 `Start-Process powershell -Verb RunAs -Wait` 自跑，UAC 用户点「是」。

**2026-08-26 渠道限流准入闸门 v2（task_045，GPT Extended 评审后推翻 v1 实施）**：`rate_limit.py` 从纯台账升级为原子 admission gate——①`try_acquire(cid,model,key)` 同一把 threading.Lock 内 清窗口→判断→预占（v1「先查再 hit()」有并发穿透）；②滞后状态机整数门槛 trip=ceil(95%)/resume=floor(85%)（20→19/17），不用固定冷却；③429 只学 cooldown 不学永久 RPM：Retry-After 优先（cap 600s）否则指数退避 15/30/60/120/300s，2xx 清 consec429 不清 blocked_until；④scope 粒度：xiaohongshu=channel 一桶、openrouter free=credential 每 key 一桶（池内 key 是不同账号各算各的），free 池加 `|free` 后缀分桶防付费模型误伤；⑤channels.chat_completion key 循环内每次真实 HTTP attempt 前 try_acquire，某把 key 拒只跳该 key、全拒才抛 RateLimitSkip→route_completion 记错走下一渠道（用户顺序永不重排）；⑥24h 日额度落盘 `data/search_gateway/rate_limit_day.json`（tmp+os.replace 原子，锁内懒加载重建），1m/1h 纯内存；⑦事件只在状态翻转记一条（deque maxlen=50）。前端台账 6→8 列 + tfoot 状态翻转事件；`GET /api/rate-limits` 返 {channels,events}。生产已验证真实触发（modelscope 上游 429→熔断 backoff→自动切下一渠道客户端无感 200）。OpenRouter 正确口径：free 20/min+50/day total，曾购 $10 credits（一次性门槛）则 1000/day。commit 8fae787 已 push。方案文档 `docs/rate_limit_switch_plan.md`。

**2026-08-26 重启后回归修复：/api/search_json 端点缺失（DSH hub-web-search 回归）**：
unified_gateway.py 接管 :3000 后漏带了旧 search_gateway.py 的 `/api/search_json` 路由（DSH hub-web-search 插件调用 `GET {baseURL}/api/search_json?q=&engines=`），所有未知路径落入兜底分支返回 `{"status":"Universal AI Hub Running"}` 假响应（无 records 数组）。已补上与 `/api/search_aggregate` 同源的实现（调用 content_pool.run_search，内联返回各引擎 answer），commit b4de5fb 已推送。同时更新头部文档 7→8 引擎、登记新端点。注意：重启后下次 /api/health 首次探测全部 8 引擎可能耗时 ~24s（引擎超时探测），期内前端感觉「点不动」——等探测完成即恢复。

**2026-08-27 Claude Code 前缀模型 id 路由（provider:model 通用解析）**：Claude Code 的 settings.json 用 Cherry Studio 自定义模型格式 `provider:model`（渠道前缀如 `opencode:deepseek-v4-flash`、provider UUID 前缀如 `3f3af7c6-…:deepseek-v4-flash-vision-exp`），网关原路由不认识 → 掉 DEFAULT_CHAIN 全渠道 40x，表现为「工具全禁」。tools 转发本身一直是通的（curl 直测带 tools 正常返回 tool_calls）。修复：channels.py 新增 `_resolve_prefixed_model(q)`——首段冒号前若是渠道 id 或 sync_cherry.CHERRY_MAP 反查的 provider UUID 就剥前缀+钉定该渠道；`z-ai/glm-5.2:free` 这类天然冒号名前缀不匹配、不受影响。钩在 `model_providers()` 顶部。**坑：do_POST 把 _handle_chat 内任何异常掩码成「请求体不是合法 JSON」——看到这个错别信错误文案，真因可能在路由层**（初版漏了无冒号守卫导致普通模型全崩）。无冒号守卫必须有。验证：三种前缀全通 + 统一组/普通名回归通过。

**2026-08-27 回归实测 + /api/health 缓存落地（郭老师拍板：:3000 最终形态=AI 调用的无头搜索工具，保留后端砍前端）**：
- **架构决策**：hub_page.html 前端封存不再维护（引擎选择「全选」按钮任务作废）；:3000 保留机器接口——`/api/search_json`（DSH web_search 插件）、`/api/search_aggregate`、`/v1/chat/completions`（yuanbao-search 真实检索）等，定位为 DeepSeek/DSH 等 AI 的网络搜索工具。
- **health 缓存（commit fad1bbd）**：60s TTL 内存缓存（`HEALTH_CACHE_TTL` env 可调），`?refresh=1` 强制重探；命中返回带 `cache:{hit,age_s}` 字段。首探 ~2.7s → 命中 0.005s（改造前每次固定 ~24s）。**重启 :3000 注意：启动要串行探测 8 个引擎才绑端口，~1 分钟属正常，别提前判失败**。
- **元宝提取器大坑（关键教训）**：`run_cli` 把 JS 换行折叠成空格以绕 cmd 截断 ⇒ JS 里任何 `//` 行注释都会吞掉拼接上来的后续代码（SyntaxError: Unexpected end of input）⇒ opencli 静默 fail ⇒ extract_answer 永远 found:false ⇒ ask_engine 90s「等待回答超时」，而 health 探测只查输入框所以照常绿。YUANBAO/DOUBAO_EXTRACT_JS 的注释已全改 `/* */`。**规则：inject 进 run_cli 的多行 JS 一律禁止 // 注释**。次坑：兜底取 markdown 容器曾取*第一个*块——老会话里那是旧消息恒等于 baseline 也致超时，已改从末尾取最新块。修复后 fast 模型判读双 PASS（text_searcher qianwen 母本 403 词带 ck12.org 出处 exit 0 + 元宝 SSE 全 delta 流）。
- **测试方法教训**：不要并行跑两个都驱动同一浏览器会话的测试（text_searcher 与 yuanbao-search SSE 探测互抢元宝标签页会互相造成「未找到发送按钮」假故障）；另外后台标签页会被休眠致 DOM 空，opencli eval 一次即可唤醒。

**2026-08-27 新引擎四连实测结论（task_056，fast 模型判读）**：grok/perplexity/metaso 三引擎 PASS（真实带出处答案），zai 机制修好但站点延迟不稳（偶发超时属站点侧，非代码）。`/api/health` 60s 缓存落地后首探约 2.7s 命中 0.005s。渠道有效期标注落地：`channel_expiry.json` 数据 + api_page 到期徽标（≤7 天橙、已过期红、ark 标「调整中」）。cores 全在 commit `8ec04de`。

**2026-08-27 text_searcher 检索阶段修复（#58，重点进展）**：`_ask_engine_slow` 增加 `engines.engine_health(eid)` 自愈闸门（会话未就绪先 3× 轮询自愈，仍失败即返回错误码不空等）；`_reuse_existing` 复用现有答案避免重复提问污染会话。检索 4 引擎（kimi/qianwen/doubao/yuanbao）真实失败模式：kimi=排队弹窗致 300s 超时、qianwen=滑块验证码需人工拖、doubao=「该页面暂时不可用」错误页、元宝=false「未找到发送按钮」（实际已答，`_reuse_existing` 救回）。**实测修复路径：元宝复用现有答案命中**——255 词英文母本 + 真实出处 `https://fliphtml5.com/vhlrd/ciss/1E___Photosynthesis_reading-grade_8/`，「① 检索」「② 选定母本」两阶段通过，卡在 ③ 镜像站设题（见上条镜像迁移）。重跑命令：`python text_searcher.py "how do plants make food photosynthesis" --task 选词填空10选10 --words 180`。

**2026-08-27 镜像站整体迁移（重大变更，text_searcher 适配器作废）**：镜像站前端整体改版——旧域名 `vip-23.67673.live` 已停用，横幅明示「网址aiwenda.chat已停用」，实际跳转 **`ai.wendabao-f.net`（AI问答宝）**。新 UI = Vue + Naive UI（n-button），「选服务卡(ChatGPT/Claude/DeepSeek/Gemini/Grok) → 选 Plus 账号卡(GPT-5 ⑫⑳…活跃) → 进对话」两段式；已登录、会员有效至 2026-10-28。**后端 API 仍是老链路**：每张 Plus 卡轮询 `api.67673.live/endpoint?carid=vip-NN` 取健康徽章（如 `{"label":"GPT-5 ㉓","isHealthy":true}`）——镜像后端并未迁，只会壳换了。**新 UI 卡点（已排查多轮，未解决）**：新对话页 DOM 79KB 里**没有任何 composer 标记**——textarea/contenteditable/发送按钮全不存在，连 placeholder/input/prompt/composer 字符串都扫不到；点 Plus 账号卡不跳转（window.open 劫持捕捉不到，card click 走 Vue 路由但渲染空壳），点「新对话」按钮只换 URL hash（`#/chat/<id>`）不渲染输入框。结论：**text_searcher 镜像站设题适配器已整体失效**（JS_EDITOR_PROBE/JS_SEND/JS_MODEL_ARROW/JS_CLICK_THINKING/JS_VERIFY_EXTENDED/JS_LAST_ASSISTANT 全是老 ChatGPT 界面选择器）——需郭老师先在新站确认「怎么真正进对话」，或改用别的设题通道；text_searcher ③ 镜像站设题阶段当前阻塞。

**2026-08-30 注意点（用户指示，暂不处理）**：:3100 API 转发网关的**路由日志中未看到任何调用记录**——每次调用 :3100 后应先检查其路由日志确认走了；当前任务多且复杂，先记好此点，等所有任务做通、流程稳定后再排查（可能与路由日志路径/记录机制有关）。

**2026-08-31 派发中心二级页面 + dashscope 渠道删除**：
- **派发中心二级页面**：api_page.html 导航新增「派发中心」二级页（hash `#/dispatch`，VIEWS 加 dispatch、`.ni` 导航按钮 + iframe 嵌 `/dispatch`）——dispatch.py 三级派发矩阵观测页（A/B/C 执行位徽章 + 历史表，15s 轮询 /api/dispatch/status）。在真实浏览器已验证全绿。
- **dashscope（阿里云百炼）渠道全量删除**（用户拍板）：内置渠道删除须改 channels.py（渠道块+CHANNEL_ORDER/DEFAULT_CHAIN/docstring）+ 数据文件（routing.json 链 / unified_models.json 成员+display / channels.json 明文密钥 / channel_models / channel_notes / model_pricing / channel_expiry）+ sync_cherry.py CHERRY_MAP，再普通权限重启网关。7 个 JSON 全校验通过、py_compile 通过、/api/health 19→18 渠道、deepseek-free 真实请求转发放行（→bai deepseek-v4-flash-vision-exp）。详见 ai-hub-memory CHANGELOG S-20260831-02。

**2026-08-30 火山方舟 Coding Plan 套餐接入（49.9元/月，用户已购）**：
- **网关单渠道 `ark-coding`**（**一个厂商一个渠道，别拆多渠道**——用户 2026-08-30 纠正过我拆 3 渠道的做法）：models=[deepseek-v4-flash, glm-5.3-flash, deepseek-v4-pro]，base=`https://ark.cn-beijing.volces.com/api/coding/v3`（OpenAI 兼容；**⚠️勿用 /api/v3 会额外扣费**）；key 与模型服务 ark key **不同**（`ark-dcc1…-811ab`，coding 套餐专用，页面「配置API Key」步骤获取，点击「选择使用」即复制）；billing=subscription（付费 rank，不抢免费链）。unified 组 `coding-plan`={ark-coding: deepseek-v4-flash}（flash 走组名；glm-5.3-flash / deepseek-v4-pro 按模型名直调或前缀钉定 `ark-coding:glm-5.3-flash`）。**编排第 4 组 `glm-5.3-flash`**={opencode→ark-coding}（用户 2026-08-30 定：OpenCode 第一、方舟 Coding 第二；openrouter/zenmux 收钱已剔除）。实测 glm-5.3-flash 在 ark-coding 通（返回 glm-5-3-flash）。
- **ark 与 ark-flash 合并（2026-08-30 用户：只用 ark 免费额度 + coding 套餐）**：ark-flash 渠道删除，GA 模型并入内置 ark 渠道（channels.py `ark.models` 加 deepseek-v4-flash-ga-260731 / deepseek-v4-pro-ga-260813；`_merge_custom_into_globals` 用 setdefault 不覆盖内置，故改 channels.py 代码）。flash-ga 按模型名直调（包含搜索命中 ark+ark-coding，免费 ark 优先）。火山方舟只剩 ark(免费)/ark-coding(套餐)。**deepseek-free 链 8 成员（2026-08-31 免费优先付费垫底调整后）**：routing order=[nvidia,bai,openrouter,ark,sensetime,siliconflow,modelscope,**opencode(付费垫底)**]；display="DeepSeek 免费链（nvidia→B.AI→openrouter→ARK→商汤→硅基→魔塔→OpenCode 付费垫底）"。唯一付费=opencode(Go 订阅)。详见 CHANGELOG S-20260831-03。
- **网关重启免 UAC（2026-08-30 用户明确要求记录，防其他 Agent 反复授权）**：网关必须**普通权限**运行，重启全程免 UAC。正确姿势（普通 PowerShell）：
  `Set-Location D:\项目\services\search_gateway` → `Stop-Process -Id <pid> -Force`（普通权限可杀，因进程是普通权限）→ `Start-Process python -ArgumentList '-u','api_gateway.py' -WorkingDirectory D:\项目\services\search_gateway -WindowStyle Hidden -RedirectStandardOutput ... -RedirectStandardError ...`。
  **禁止** `Start-Process -Verb RunAs` 启动 python（会把网关变提权进程 → 下次杀它必 Access denied → 恶性循环反复弹 UAC）。若遇到提权进程（普通 Stop-Process 报 Access denied）：一次性提权只**杀**旧进程，再**普通**启动新的；绝不用提权脚本同时重启。已实测普通权限杀/启全通过（pid 48152）。
- **CC Switch（cc-switch.db providers 表）**：provider「火山方舟 Coding」**直连官方** Anthropic 端点 `https://ark.cn-beijing.volces.com/api/coding` + coding key + 3 模型（FABLE/SONNET=flash、OPUS=pro、HAIKU=glm，apiFormat=**anthropic**，meta `{"commonConfigEnabled":false,"endpointAutoSelect":true,"apiFormat":"anthropic"}`）。用户要求**两种方式分开**：网关（OpenAI 转发）+ CC switch（直连官方），勿混。
- **网关统一剥离 reasoning_content（2026-08-30 用户拍板）**：魔塔 DeepSeek V4 Pro 输出断续根因=thinking 模型 reasoning_content 逐 token 流式、网关原样透传、客户端当正文显示（实测每 SSE 事件 1-4 字，max_tokens=30 时 content 空/思考占满 finish=length）。api_gateway.py 新增 `_del_reasoning_keys`/`_strip_reasoning_json`(非流式)/`_SseReasoningStripper`(流式逐行剥离、跨 chunk carry 容错、\r\n 归一)，非流式 `_strip_reasoning_json(_normalize_content_filter(result.read()))`。剥离后 SSE 无 reasoning_content，thinking 期间客户端静默等正文。**无 max_tokens 默认**（透传客户端值）。

- **deepseek-paid 付费链（2026-08-30 给 Cloud Code，同 deepseek-free 一个统名多渠设计）**：`:3100` unified 组 `deepseek-paid`={ark-coding:deepseek-v4-flash → opencode:deepseek-v4-flash}，**套餐优先**（ark-coding=火山方舟 Coding Plan 排第一）；routing 规则 `deepseek-paid order=[ark-coding,opencode]`。`glm-5.3-flash` 组={opencode→ark-coding}，routing **两组成员顺序相反需靠 per-model routing 规则**（`_apply_routing_rule` 严格按 `order` 重排，勿改全局 CHANNEL_ORDER——global rank 会让 opencode 恒先，且无法同时满足两组相反顺序）。**`free` 字段无逻辑读取，排序/展示/着色全认 `billing_type`**（"subscription"→付费）。**并行写入抹掉事件**：某 cleanup Agent 10:11 把 ark-coding 从 channels.json 删掉+砍 glm/deepseek-paid 成员为 opencode-only+删 coding-plan 组；已从 `channels.json.bak_del_ark_101118`/`unified_models.json.bak_del_ark` 恢复（完整 key `<ARK_CODING_KEY>`）；确认磁盘持久化 4 组（fast/free/paid/glm，coding-plan 已按「刚好4个」口径删除）且 DSH 同步 4 模型。**cc-switch gateway-3100 主模型槽 ANTHROPIC_MODEL=deepseek-paid**（用户拍板主槽改付费链，其余槽位原样保留）。
